---
import ResumeSection from "@/components/ResumeSection.astro";

import { getFormattedAbout } from "@/lib/dates";

const about = await getFormattedAbout();
---

<ResumeSection title="About" class="grid gap-3">
  <p class="font-space-mono">{about}</p>
</ResumeSection>
