---
import Box from "./Box.astro";

interface Props {
  class?: string | undefined;
}

// const bodyPrimaryStyles = "text-neutral-900 dark:text-white";
// const bodySecondaryStyles = "text-neutral-700 dark:text-white/80";

const { class: className } = Astro.props;
---

<Box
  class={`
    ${className ?? ""}
    relative rounded-2xl shadow p-4 overflow-hidden
  `}
>
  <slot />
</Box>
